<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.final_groupproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_groupProject">
        <activity android:name=".FavouriteEvents"></activity>
        <activity android:name=".ticket_help_alert" />
        <activity android:name=".The_Audio_Database_api" />
        <activity android:name=".Covid19_case_data" />
        <activity android:name=".Recipe_Search_page" />
        <activity android:name=".TicketMaster_event_search" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>